#include <stdlib.h>
#include <stdio.h>

/**
 * sub - a function that substrats two integers
 * @a: first integers
 * @b: second integers
 * Return: The value of the addition
 */
int sub(int a, int b)
{
	return (a - b);
}
