#include <stdlib.h>
#include <stdio.h>

/**
 * add - a function that adds two integers
 * @a: first integer
 * @b: second integer
 * Return: The value of the addition
 */
int add(int a, int b)
{
	return (a + b);
}
