#include <stdlib.h>
#include <stdio.h>

/**
 * mod - a function that modifirs two integers
 * @a: first integer
 * @b: second integer
 * Return: The value of the modification
 */
int mod(int a, int b)
{
	return (a % b);
}
