#include <stdlib.h>
#include <stdio.h>

/**
 * mul - a function that multiplies two integers
 * @a: first integers
 * @b: second integers
 * Return: The value of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
