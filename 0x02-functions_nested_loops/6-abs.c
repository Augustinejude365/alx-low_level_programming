#include "main.h"

/**
 * _abs - A function that prints the absolute value of an integer
 * @n: n is an integer
 * Return: An integer
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
