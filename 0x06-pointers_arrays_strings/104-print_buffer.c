#include "main.h"
#include <stdio.h>

/**
 * print_buffer - prints buffer
 * @b: buffer
 * @size: size
 * Return: void
 */
void print_buffer(char *b, int size)
{
	imt k, l, m;

	k = 0;

	if (size <= 0)
	{
		printf("\n");
		return;
	}
	while (k < size)
	{
		l = size - k < 10 ? size - k
			printf ("%08x", k)
			for (m = 0; m < 10; m++)
	}
			
